# Note-oriety
A light, simple, no-nonsense note taking app.

Use Ctrl-S to save your note when you are done typing.

The .exe is self-containing and so is the only thing required to use the software (it will create a notes folder in whatever directory it is placed).

The .pyw is the same as the .exe but is the Python file before being compiled.
